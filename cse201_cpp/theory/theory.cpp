#include<bits/stdc++.h>

int main () {
    // new, delete
    int* arr = new int[10]; // allocating array
    delete[] arr;
    std::vector<char> *pv = new std::vector<char>(); 
    delete pv; // alloc and dealloc one object
    

}